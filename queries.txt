CREATE TABLE Foods(name TEXT NOT NULL PRIMARY KEY, cal INT NOT NULL, fat REAL NOT NULL, carb REAL NOT NULL, protein REAL NOT NULL);
CREATE TABLE Log(day TEXT NOT NULL, time TEXT NOT NULL, food_eaten TEXT NOT NULL, portions INT NOT NULL, FOREIGN KEY(food_eaten) REFERENCES Foods (name));
CREATE TABLE Foods_eaten(name TEXT NOT NULL, portions INT NOT NULL)